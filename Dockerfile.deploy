FROM hashicorp/terraform:1.16.0 AS terraform

FROM node:22-alpine

RUN apk add --no-cache docker-cli

COPY --from=terraform /bin/terraform /usr/local/bin/terraform

WORKDIR /workspace

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

ENV CI=1

CMD ["node", "scripts/deploy.mjs"]
